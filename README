This is the readme file.

Run the project using the following commands:

docker-compose up --build

In another terminal run these command to create the database tables and seed the category data:

docker exec teebay_server npx prisma db push
docker exec teebay_server npm run seed
